package CarService;

public class CarInfoService {   /** Перенесли данный метод в отдельный класс, так как нарушался принцип ответственности */
     
    public void getCarInterestInfo(String carType) { /** метод при получении и ожидания что будет выдан авто класса - Сидан */
        if (carType.equals("sedan")) {

        }
        if (carType.equals("pickup")) { /** метод при получении и ожидания что будет выдан авто класса - Пикап */

        }
        if (carType.equals("van")) { /** метод при получении и ожидания что будет выдан авто класса - Минивэн */

        }
    }


}
